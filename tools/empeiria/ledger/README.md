# ledger

One file per cvc5 issue worked, `<number>.md`, written by
[`run_empeiria`](../run_empeiria).

Each holds what this project produced (`TRIAGE:`, `WHAT I DID:`,
`UNSURE ABOUT:`) and, once a maintainer has answered, what they actually did
(`HUMAN RESPONSE:`, filled by `run_empeiria --record N`).

**The delta between those two is the only thing here that is an asset.** The
rest is bookkeeping that makes the delta legible.

Nothing in this directory is sent anywhere. It is a record for a person to read
and decide about — see [`../../../docs/pr-policy.md`](../../../docs/pr-policy.md).
